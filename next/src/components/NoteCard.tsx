interface NoteProps {
  title: string;
  description: string;
  id: string;
}

import React from "react";

const NoteCard = ({ description, id, title }: NoteProps) => {
  return <div>
    
  </div>;
};

export default NoteCard;
